[
  {
    "name": "/setprefix",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "📝 Current prefix: None\n\nSend a new prefix to set it, or send /cancel to keep the current one."
            }
          }
        ],
        "send": {
          "text": "/setprefix"
        }
      }
    ]
  }
]
