[
  {
    "name": "/status",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "👋 Welcome! Tap a button below to get started." } }
        ]
      },
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "📊 No alerts yet. Configure your TradingView webhook to start receiving alerts."
            }
          }
        ],
        "send": {
          "text": "/status"
        }
      }
    ]
  }
]
